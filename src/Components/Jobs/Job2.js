import React from 'react'
import { Typography, Card, CardActions, CardContent, CardMedia, CSSBaseLine, ToolBar, Container } from '@material-ui/core'
export default function Job2({ id }) {
    return (
        <Card>asd</Card>
    )
}
