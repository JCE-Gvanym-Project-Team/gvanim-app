import { makeStyles } from '@mui/styles'
import colors from '../../../indexStyle'

const sortStyles = makeStyles(theme => ({
    container: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'left'

    }

}));


export default sortStyles;